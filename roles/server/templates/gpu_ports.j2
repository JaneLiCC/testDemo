network:
  renderer: networkd
  ethernets:
{% for int in devices %}
    {{ int }}:
      addresses:
        - {{ devices[int].ip }}
      routes:
        - to: 0.0.0.0
          via: {{ devices[int].route.nexthop | ipaddr('address') }}
      dhcp4: no
{% endfor %}
